import { ThreeDots } from 'react-loader-spinner';

export const Loader = () => {
  return (
    <ThreeDots
      color="#fefefe"
      wrapperStyle={{
        justifyContent: 'center',
      }}
    />
  );
};
